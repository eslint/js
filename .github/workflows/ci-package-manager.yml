name: ci-package-manager

on:
    push:
        branches:
            - main

    pull_request:
        branches:
            - main

permissions:
    contents: read

jobs:
    ci-package-manager:
        uses: eslint/workflows/.github/workflows/ci-package-manager.yml@main
