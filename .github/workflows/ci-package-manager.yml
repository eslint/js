name: ci-package-manager

on:
    push:
        branches:
            - main

    pull_request:
        branches:
            - main

jobs:
    ci-package-manager:
        uses: eslint/workflows/.github/workflows/ci-package-manager.yml@3030377848e4c0b9c28a271b88a5c8c57359da68 # main
